services:

  backend:
    build: 
        context: .
        dockerfile: Dockerfile_backend
    volumes:
        - ./src/backend:/app/src/backend
        - ./src/models:/app/src/models
    ports:
        - "8000:8000"
    command: uvicorn src.backend.api:app --host 0.0.0.0 --port 8000
